# AGENTS.md — crescent-city/.github

GitHub configuration for the DEPRECATED `crescent-city` repo (superseded by
`crescent-city-intel`; see its README). Contains `workflows/` only.
Do not add new automation here; the tree is retained for historical reference.