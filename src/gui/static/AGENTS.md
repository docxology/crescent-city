# AGENTS.md — crescent-city/src/gui/static

Static front-end for the deprecated `crescent-city` GUI.
- `index.html` — "Crescent City Municipal Code" browser (Chart.js + marked
  via CDN, single-file inline CSS/JS).
- `docs.html` — "Crescent City API Documentation" page.
Self-contained HTML; no build step. Historical — the active platform's
pages live in `crescent-city-intel/src/pages/`.