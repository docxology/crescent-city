# MANUSCRIPT_STATUS

## Repo type

Deprecated civic-intelligence web platform. `README.md` states the repository
is DEPRECATED and superseded by `crescent-city-intel`; the tree is a
TypeScript/Bun application (`src/`, `scripts/`, `tests/`, `openapi.yaml`),
not a publication-track research project.

## Evidence checked

- `README.md` (deprecation banner, platform description)
- `docs/` (API reference, architecture, configuration, roadmap — no manuscript content)
- Top-level tree: no `manuscript/`, no analysis-output or figure pipeline

## Why no publication-target manuscript applies today

The repo produces a website/application, not research claims; active
publication work has moved to the successor repo `crescent-city-intel`
(which carries its own complete `manuscript/` directory).

## What would trigger creating one

Only if a research output were split back out of this repository; otherwise
all manuscript work belongs in `crescent-city-intel`.
